"use strict";
{
    const fileEx = ["js", "ts"];
    const cars = [{
            make: "make",
            model: "model",
            year: 10
        }];
}
// tuples
{
    let myCar = [30, "kd", "kdah"];
    const numPair = [4, 5];
    numPair.push(43);
    numPair.pop();
    numPair.pop();
    numPair.pop();
}
